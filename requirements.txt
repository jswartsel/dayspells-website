# extraction pipeline
numpy>=1.24
opencv-python>=4.8
Pillow>=10.0
scikit-learn>=1.3      # palette clustering only; build.py degrades without it

# headless checks (optional)
playwright>=1.40       # then: playwright install chromium
